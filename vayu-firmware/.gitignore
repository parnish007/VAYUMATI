# Firmware-specific secrets — copy config.h.example → config.h and fill in your values.
# Never commit the real config.h — it contains WiFi passwords.
node-a-air/config.h
node-b-soil/config.h

# Arduino build artifacts
*.elf
*.bin
*.hex
*.map
build/
.pio/
